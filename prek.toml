minimum_prek_version = "0.3.2"
default_install_hook_types = ["pre-commit"]

[[repos]]
repo = "local"
hooks = [
  { id = "nix-fmt", name = "nix fmt", language = "system", entry = "nix fmt", files = "\\.nix$" },
]
